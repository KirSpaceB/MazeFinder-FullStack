/**
 * @jest-environment jsdom
 */

 // how do we code this hit xD
 describe('Undo Button', () => {
  it.skip('Button undos previous action', () => {
    let previousGrid = function(loadButton) {
      loadButton = previousVersionOfGrid
    }

    let y = 4;
    
  expect(x(2,2)).toEqual(y);
  })
})

